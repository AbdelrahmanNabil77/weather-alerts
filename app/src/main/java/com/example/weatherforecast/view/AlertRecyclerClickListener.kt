package com.example.weatherforecast.view

import com.example.weatherforecast.model.Alert
import com.example.weatherforecast.model.Weather

interface AlertRecyclerClickListener {
    fun deleteAlert(id:Int)
}